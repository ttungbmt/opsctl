#!/usr/bin/env bash
#MISE description="Stop the test container, keeping the node_modules volumes"
#MISE alias="down"
set -euo pipefail
cd "${MISE_PROJECT_ROOT:-$(dirname "${BASH_SOURCE[0]}")/../../..}"
exec docker compose down
